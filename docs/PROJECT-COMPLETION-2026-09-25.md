# Total completion ledger — 2026-09-25

This ledger covers every open AHDNative issue and roadmap row. It records work that has actually landed and the evidence still needed; an open issue count is not a count of untouched work. The target is the existing React/Tauri app, with offline iOS SP first, the AHDGame server for MP, and the current AHDGame/AHDClient player journey and identity as references. No row is removed from scope by its priority or former `status: deferred` label.

## Landing order and evidence gates

1. Reconcile current `main`, open PRs, stranded commits, and worktrees. Preserve dirty and locked worktrees while inspecting their contents. A patch already on `main` is not replayed.
2. Build the independent AHDGame-versus-Native turn comparison and phase-trace gate (#117, #281, #28). Pin reference revision, scenario, action sequence, RNG inputs, expected phase results, and an executable diff. Each later mechanics claim must use that gate or state its exact uncovered boundary.
3. Establish the current Client/Game SP snapshot contract separately from historical v42 (#122). Complete reference export, Native import/export, Game import, and bidirectional continuation (#301–#304); complete progressed political transfer through the owner-approved versioned successor with a compatible turn engine (#116), while keeping historical v42 projection bounded and fail-closed. Reject unsupported or corrupt input clearly.
4. Close engine subsystems against those independent gates: corporate/union/bank/market mechanics (#107, #109–#111, #114–#115, #211, #297–#299, #308, #322); country law catalogs (#101, #284–#287); elections, parties, caucus, regional legislature, and ministers (#57, #59, #61, #64, #91, #95–#97, #105, #262–#263); macro and international turns (#79, #106).
5. Close player-facing flows with reference-source notes, integrated action tests, and actual MP/SP visual comparison at 320px and 390px: identity and imagery (#143, #148, #355), destinations and region/military views (#54–#55, #72–#73, #75), finance and markets (#76–#77), profile and creation (#48, #51, #240), and playable content (#118). Preserve the approved globe landing, side drawer, bottom primary navigation, and compact resource footer.
6. Verify the actual offline SP create/action/turn/save/relaunch/continue loop, corrupt-save handling, deterministic replay, supported-country matrix, Android/Windows packages, and private iOS candidate record (#124). Complete MP against the existing server and WebView-owned authentication, then its authenticated device acceptance (#53, #86, #363). Prepare #43 and #44 with local replay, lifecycle automation, benchmark scenarios, commands and expected outcomes; leave only actual physical-device results unchecked until measured.

For each implementation slice: record the reference behavior and public player or engine contract, show a failing test, implement the smallest behavior, show it passing, run the relevant integrated flow, then update the issue, parent counts, and roadmap. Visual work also needs source-screen comparison. Local compilation is not device validation. Paid Codemagic builds remain iOS-only, private, and gated by local evidence under the 20-minute workflow cap.

## PR landing and stranded branch

| Item | Decision and evidence |
| --- | --- |
| #670 | Merged as `86ca568`; hosted `verify` passed. `gh` could not delete its local branch because a worktree used it; the merge itself succeeded. Its `Fixes #436` keyword prematurely closed #436 despite outstanding device and rendered-reference criteria. #436 was reopened with a corrective evidence comment and remains `status: partial`. |
| #671 | Merged as `4a1a299`; hosted `verify` passed. #76 remains open for its other acceptance criteria. |
| #672 | Merged as `ff37f14`; hosted `verify` passed. #118 remains open for missing content. |
| #674 | Merged as `c33ecf5`; hosted `verify` passed. #299 and parent #211 remain open for their remaining integration and visual criteria. |
| #673 | Still open. The earlier hosted failure was repaired on the PR branch with accessible era radios and a focused selector correction. The new hosted run passed `verify` and engine/content tests but has remained in `test:smoke` since 03:29 UTC; do not merge before the full gate completes. |
| `fix/ahd-native-issues` | **Already merged; do not cherry-pick.** At the fetched `origin/main`, `git merge-base --is-ancestor ee0433d origin/main` succeeds, and `git rev-list --left-right --count origin/main...fix/ahd-native-issues` reports `392 0`. Its clean worktree was removed. Earlier divergent counts were superseded by the complete fetched graph. |

## Issue reconciliation in this batch

- #117: #279 and #280 checklist entries are verified closed, with merged PRs #289 and #331 and passing hosted `verify`. The parent is `status: partial`; Native capture and the integrated cross-engine gate (#281) remain unchecked. The source and test paths are in the issue comment.
- #122: #300 checklist entry is verified closed, with merged PR #311 and passing hosted `verify`. The parent is `status: partial`; both snapshot transfer directions still say `contract-only`, and #301–#304 remain unchecked. The source and test paths are in the issue comment.
- The current SP contract previously advertised Native save schema 44 while `world.ts` exports `SCHEMA_VERSION = 48`. PR #676 merged as `2ec7c81` after hosted verify passed; it derives the advertised value from the live constant. Transfer directions remain `contract-only`.
- #116: the owner approved a versioned successor with a compatible turn engine as the completion target. The issue body and comment now state this. Pinned v42 reader/writer preservation is valid but its turn pipeline freezes `countryPolitics`; the historical projector stays fail-closed. PR #679 corrects the stale v42 evidence document and records current hashes, schema 48, and the missing parity. No successor transfer is implemented yet.
- #436: reopened after premature automatic closure by #670. Its browser fallback is delivered, while rendered reference comparison and a named physical Dynamic Island pass remain unchecked. This also means the active issue set increased from the supplied 59-issue baseline; it is not a new feature request.
- Parent #28's current summary was reconciled to the live 2026-09-25 issue and PR states: 60 open issues (32 partial, 19 not started, 2 blocked, 3 deferred, 4 tracking; five P0) and three open PRs at that instant. The old audit section remains explicitly historical. These counts describe status, not untouched effort.
- #148: canonical Native and Client logo bytes were verified identical by SHA-256 `1a7fe54f33c781d6b7741277a20a9e800ca5525a0fbea790a7109c3e119f66a9`. The recorded AHDClient commit was unresolvable; `docs/UI-REFERENCE.md` now pins the verified source commit `62ecdba085c6899d4ab6761571524b88a39cec1e`. The issue stays partial for installed-platform visual evidence; no asset was replaced.
- PR #675 carries that #148 provenance correction. It is documentation only and remains open until hosted `verify` finishes.

## Worktree safety rule

The initial inventory contained 44 registered worktrees, including the dirty detached primary checkout and this task's own checkout. No worktree reported `locked`. Use `git worktree list --porcelain`, a fresh `git cherry origin/main <branch>`, ancestry, status, and a tree comparison to decide each disposition. Dirty worktrees require content review before removal; clean branches with unmatched changes require merge or explicit supersession evidence. Do not remove a worktree used by another running agent. Record the final disposition of each path before calling the fleet resolved.

| Removed worktree | Reason |
| --- | --- |
| `fix-ahd-native-issues` | Clean; its tip `ee0433d` is an ancestor of `origin/main`. |
| `wave5-content-20260918` | Clean; `git cherry origin/main` shows its one commit patch equivalent to the merged #672. |
| `wave5-finance-20260918` | Clean; `git cherry origin/main` shows its one commit patch equivalent to the merged #671. |
| `issue-299-regional-sector-flow` | Clean; its exact head `a4c3df3` was the head of merged PR #674. |
| `wave5-dynamic-island-20260918` | Clean; its exact head `e0fd321` was the head of merged PR #670. |
| `muse-profile-ceo-51` | Clean; its single #51 owner-card patch `0353b9c` is superseded by merged PR #562 (`b52293d`), whose one extra change fixes the test navigation type. The actual CEO relation remains open in #51; no code was lost. |
| `muse-profile-ceo-51-review` | Clean; its two #51 owner-card/type patches `ba88c8c` and `2e9709e` are the same bounded work merged by PR #562. The later rendered test is separately tracked in PR #678. |
| `muse-interbank-lending` | Clean; its two #326 commits are the earlier subset of the review branch merged by PR #559. Issue #326 is closed. |
| `muse-interbank-lending-review` | Clean; tip `80d1a1e` is exactly the head of merged PR #559 (`c90db63`). |
| `muse-proprietary-bank-risk` | Clean; its #328 base patch is superseded by the review branch merged in PR #558. |
| `muse-proprietary-bank-risk-review` | Clean; tip `23f0be3` is exactly the head of merged PR #558 (`e47c7aa`). |
| `muse-sector-89` | Clean; its #89 base patch is superseded by the review branch merged in PR #564. |
| `muse-sector-89-review` | Clean; tip `83a0c4d` is exactly the head of merged PR #564. |
| `muse-nav-510-review` | Clean; tip `f25eeee` is exactly the head of merged PR #561. |
| `issue-148-logo-icon-acceptance` | Branch patch is equivalent to merged PR #350. Its sole untracked file was a 1254x1254 RGB `ahd-native-logo-candidate.png` (SHA-256 `5fbd2523…`), distinct from the required canonical AHD logo (`1a7fe54f…`); it was unused and rejected under the owner identity rule. Removed the candidate and then the clean worktree. |
| `wave4-dynamic-island-20260918` | Clean; its patch ID `a3fa0d2` exactly matches `f98ed5f`, the landing entry-shell commit included in merged PR #670. Device validation remains #436. |
| `wave2-subsidy-20260918` | Clean; its patch ID `8a76a15` exactly matches the head commit of merged PR #661 (`ff4e0f3`). |
| `fix-447-campaign-bootstrap` | Clean but three old unmatched patches from the #447 bootstrap fix. #447 is closed by merged PR #448, reinforced by #509 (source-backed 250,000 opening campaign funds) and #588 full creation/reload test. Current `world.ts` seeds 250,000; replaying this older branch would roll back newer creation changes, so its content is superseded. |
| `caucus-smoke-order` | Branch tip `7e808cd` is exactly merged PR #406, which fixes the async save/roster smoke race. Its sole untracked file was explicitly marked a temporary `GameSession` repro probe with console logs; it was copied to `/tmp/ahdnative-caucus-disband-repro.test.ts` for audit before removal. It is not a product test or unmerged implementation. |
| `muse-world-map` | Clean branch with four earlier auth/navigation commits retained separately in `auth-nav-native-mp`, followed by feature commits linked to merged PRs #388–#421. Its world-map tip `656e10a` is an earlier, narrower version of merged PR #421, whose changed-file set includes every tip file plus finance/nation/world integration. #73 remains open for Hall of Fame and geography, not for an unlanded copy of this route. |
| `muse-news-event` | Clean branch shares the preserved auth/navigation base and merged feature chain. Its final event-detail commit `36ab4c3` changes the same four files as merged PR #422, which adds the complete local event-detail flow with persistence, back/focus and keyboard evidence; issue #78 is closed. The branch copy is superseded. |
| `muse-party-assets` | Clean branch shares the preserved auth/navigation base and merged UI chain. Its final `662f2bc` authored-color preview slice was rebased and merged in PR #408; the branch-only MobileNavigation count adjustment reflected older topology and is not current functionality. No party image asset was imported because source rights were unresolved; #355 remains open for authored-URL logos. |
| `muse-imagery-next` | Clean; four unmatched auth/navigation commits remain in `auth-nav-native-mp`. Its chart tip `6841000` was the stacked #385 approach; merged PR #495 explicitly superseded stacked PR #389 with the established shared offline TrendChart on current main, with 320/390 and accessibility evidence. #385 is closed. |
| `muse-hos-banner` | Clean; four auth/navigation base commits remain in `auth-nav-native-mp`. Its HoS setup notice `9443f7c` is superseded by merged PR #396, and office banner `d9e23b2` by merged PR #398; those PRs include the same affected screens plus source-grounded copy tests and current-main fixes. #240/#243 remain tracking/partial for wider parity. |
| `muse-imperial-redirect` | Clean; its three final #54 profile-gate/app-wiring/test commits were consolidated in merged PR #409. PR #409 covers the engine, worker, App and Profile route files; the branch-only MobileNavigation count was stale topology. Earlier feature chain was merged in PRs #388–#422; auth/navigation base remains separately. #54 stays open for its wider flow. |
| `muse-creation-complete` | Clean child of the imperial branch; its final one-party warning commit `26aa57a` is patch-equivalent to current main (`git cherry` reports `-`) and the current-main implementation is merged PR #425. The imperial parent content was already consolidated by PR #409. #242/#240 remain open for broader creation parity. |
| `muse-founding-lifecycle` | Clean stacked branch. Its final `0f2deb2` founding engine lifecycle was consolidated in merged PR #432, whose files are a superset and whose body records the same bounded opt-in cycle-0 behavior; earlier badge/feature commits were merged in preceding PRs. #223 remains open for default enablement, seeding and rendered acceptance. |
| `muse-mp-admin` | Clean stacked branch. Its final admin status/review commits `358964a`, `c2178cb`, `227a6ed` were consolidated in merged PR #399, which covers their files plus endpoint/UI tests and preserves server-gated admin permissions; earlier batch/drill-in slices were merged separately. #359/#363 remain partial for broader authenticated MP/device acceptance. |
| `muse-mp-chat` | Clean stacked branch. Its mail UI and test commits `df37377`/`90725b5` were consolidated in merged PR #403, whose files are a superset and whose body records authenticated inbox/sent/compose/reader/report/remove flows. Earlier batch and drill-in commits were merged separately; auth/navigation base remains in its own worktree. #359/#363 stay partial. |
| `auth-nav-native-mp` | Clean; tip `230d5ab` and all four ostensibly unmatched auth/navigation commits are exact ancestors of merged PR #367 head `b7c6d58` (`git merge-base --is-ancestor` exit 0). `git cherry` reported plus because the hosted merge squashed/rebased the stack; no source content remains stranded. |
| `issue-314-line-of-credit` | Branch tip is an ancestor of main; sole untracked item was an 82-line early `playerLineOfCredit.test.ts`. Merged PR #551 contains the implementation and a 338-line committed test of the same public turn/save boundary. The shorter duplicate was removed, then the clean worktree. #314 is closed; no unique behavior was dropped. |
| `muse-character-creation-audit-next-20260918` | Dirty edits were misnamed #328 prop-book save validation: three retail-charter corruption cases and the corresponding `save.ts` guard. Current main contains the same cases and exact guard/message via merged PR #646. Its branch patch was already equivalent to main (`git cherry -`). Restored duplicate dirty edits, then removed the clean worktree. |
| `release-preview-0.1.9-20260918` | Branch tip is an ancestor of main. Reviewed dirty state: Cargo.toml only added explicit empty `features = []` to existing Tauri dependencies (no behavior/version change); sole untracked item was `node_modules.shared` symlink to the shared dependency directory. Restored the no-op edit, removed the symlink, then removed the clean worktree. No private artifact or signing material was copied or published. |
| `release-preview-0.1.9-4-20260918` | Branch tip is an ancestor of main. Its only dirty change was the same no-op explicit empty Tauri feature list; restored it and removed the clean worktree. No private artifact was present in its tracked diff. |

The registered count subsequently fell from 44 to 22 registered including one new temporary benchmark worktree. Thirty-four original removals above were performed in this task. Four more paths disappeared concurrently: `muse-account-link-crash`, `muse-auth-parity-audit`, `muse-union-phase-323`, and `muse-wallet-portfolio-redesign`. The latter two still have local branches that are ancestors of `origin/main`; the first two no longer have local branches. Their removal was not performed or validated by this task, so final fleet reporting must distinguish them. The remaining 6 original paths plus one temporary benchmark path are not classified as resolved by this ledger.

`muse-profile-ceo-51` contains a clean unmatched #51 Profile ownership-card commit. It is not deleted: the branch records the AHDGame CEO-only reference and explicitly documents Native's missing CEO/salary/dividend state. Its exact tip was pushed as `handoff/profile-owner-card-51` for a bounded TDD integration review against current `main`; #51 remains open. The separate `muse-profile-ceo-51-review` branch has additional divergent edits and has not been declared redundant.

## Current PRs after initial five

- #675 (canonical logo provenance, #148 partial) passed hosted verify and merged as `c8191d1`. Installed-platform visual evidence remains.
- #676 (current SP schema drift, #122 partial) passed hosted verify and merged as `2ec7c81`; real transfer remains open.
- #677 (Native differential trace capture, #281/#117 partial) fixed all four review findings, passed hosted verify and merged as `a0e794f`. Inputs/phase alignment and the parity gate remain open; no parity claim.
- #678 (#51 rendered Profile owner-card smoke) corrected its direct save mutation wording and documentation paths, passed hosted verify and merged as `b5d4d03`. The issue body is partial; CEO/vacancy and income behavior remain absent.
- #679 (correct historical v42 evidence for #116) passed hosted verify and merged as `c7f0493`. #116 remains P0 partial for successor transfer and continuation.

## Remaining registered worktree inventory, 2026-09-25

A fresh `git worktree list --porcelain` found 35 worktrees before the fourteen subsequent duplicate/rejected-asset/superseded removals; 21 original paths remain, none locked. `git cherry origin/main` was run for every branch; the number of plus patches below is real unmatched patch content, not a raw ahead count. These worktrees have **not** been resolved and must not be represented as merged or deleted.

| Worktree | State | Disposition needed |
| --- | --- | --- |
| Primary checkout `/root/projects/AHDNative` | detached, 4 dirty entries | Preserve and review dirty content; outside this task worktree. |
| Task checkout `feat-ahdnative-total-completion` | PR #679 branch, untracked local ledger | Keep until PR lands and local ledger has a durable final home. |
| `glowing-era-date-selector` | clean, 3 unmatched patches | Active PR #673; keep until it lands. |
| `muse-issue-40-final` | dirty, ancestor | Review dirty content before removal. |
| `muse-proprietary-bank-risk-audit-next-20260918` | dirty, 0 unmatched / 1 equivalent | Review dirty content before removal. |
| `wave3-corporation-mechanics-20260918` | dirty, ancestor | Review dirty content before removal. |

- PR #673: `39eb17f` added a rendered player-control test for selecting week 53 through 2027-12-31 (red at 3,600 slots; green at 3,975) and `cd1122d` rejects rolled dates such as 2027-02-30 through the public `createWorld` contract (red accepted; green 3/3 focused tests). The final hosted gate is pending; no merge yet.

- Draft PR #680 adds public current-SP collection-digest verification as a #302 prerequisite. Red: function absent; green: focused contract file 5/5 locally. Hosted verify is pending. It does not change either `contract-only` transfer direction.

- Draft PR #681 prepares #43/#44 with a turn-95 public-engine replay probe and device runbook. Red CLI test (missing script), green 1/1 integrated two-sample test on Linux (111.56 s total under loaded host). Twenty-sample scheduler job `20260925T045356Z-1584b9c1` is queued from commit `9e8dcb2`; no p50/p95 result or physical-device evidence yet. Temporary detached worktree `device-gate-benchmark` exists solely for that scheduler job and must be removed after result collection.
- PR #673 now includes `cd0c44d` 320px/390px rendered selector smoke; local scheduler job `20260925T045616Z-c2e6e9f5` and fresh hosted verify are pending. Screenshots not inspected yet.

## 07:30 worktree and PR update

- #680 passed hosted verification and merged as `c34b736`. Collection digests now have a public verifier, but both transfer directions remain `contract-only`.
- #681 passed hosted verification and merged as `29ec284`. Its 20-sample Linux replay remains queued; #43/#44 phone acceptance remains unrun.
- Draft #682 (`79b68b8`) fixes the save-load portrait limit through the public save contract. Its local focused, 17/17 creation, and 15/15 historical v42 tests passed; hosted verification is pending. This is the unique save-loader edit in the dirty proprietary-bank audit worktree, with the additional distinction that #682 applies the 2 MB portrait cap instead of the older 4 MB header cap.
- `muse-issue-40-final`: branch ancestry and `git cherry origin/main` show no unmatched commits. Its only 66 dirty lines are tests: one directly inserts a ministerial order into state, and three directly inject metric rows into schema-v42 projection. Current main already has the TFP and fail-closed regional metric behavior. These tests do not provide independent AHDGame outcomes or an actual player order flow, and their projection cases largely mirror the existing guard. Discard the tests and remove the worktree; #40 is already closed and #116/#106 retain their actual gaps.
- `wave3-corporation-mechanics-20260918`: branch ancestry and `git cherry origin/main` show no unmatched commits. The dirty dividend patch is a unique unfinished attempt. Its own source note admits the reference cash-floor rule, NPP/fund/corporate recipient ledgers, CEO salary, R&D and plan-gravity state are missing. It debits a full dividend pool from corporate capital while leaving most payout value undistributed, which cannot support a parity claim. Reject this patch, remove the worktree, and keep #107 open for a source-engine-backed implementation with the missing ledgers and phase inputs. No production dividend behavior from this dirty patch is merged.
- `muse-proprietary-bank-risk-audit-next-20260918`: its sole branch commit is patch-equivalent to main via #646. Dirty creation-avatar tests and `world.ts` validation are already exactly present on main. Its dirty save guard used the header's 4 MB limit for avatars; draft #682 carries a corrected 2 MB avatar rule and public red/green save test. Remove this redundant worktree now; keep #682 open until hosted verification. #44 remains open for device durability.
- Detached primary checkout `c6510ee` has one unmatched auth-isolation commit. The old Rust navigation action and zero-capability tests are already subsumed by main's MP WebView allowlist/capability tests; the old `OnlineSession.test.ts` path was deleted on main. Ported its unique public offline SP create/action/turn/save/relaunch regression to draft #683 (`2b3d066`). The primary checkout itself is a repository anchor and still needs cleanup after #683 passes; its dirty `src/game/types.ts` is an obsolete subset of current main, and its three untracked workspace symlinks point at an already removed worktree. It has not yet been reset or deleted.

The live GitHub inventory after #302 moved to partial is 60 open issues: 34 partial, 17 not started, 2 blocked, 3 deferred, and 4 trackers. #673, #682, and #683 are the three open PRs; all hosted gates are pending. The registered worktrees are now four: the detached primary checkout, this task checkout, the #673 checkout, and the temporary benchmark checkout. The two queued scheduler jobs have not begun because the shared 18-vCPU host load has stayed above the scheduler threshold. Neither the 20-sample latency record nor 320/390px screenshot inspection has been claimed.

Issue-state audit corrected two stale `not started` labels without claiming closure: #54 is partial after merged PR #409's imperial save marker/record gate and Profile notice, but its test directly injects the save and real imperial creation/gameplay remain; #73 is partial after merged PR #421's offline world map directory, but the Hall of Fame is an explicit unavailable notice and spatial/leaderboard/link acceptance remains. Parent #28 now reads 60 open: 36 partial, 15 not started, 2 blocked, 3 deferred, 4 trackers. The other 15 `not started` issues still require their named requested slices; this audit did not relabel them based on adjacent engine types or placeholders.

#263 was also stale `not started`: its children #275/#276 are closed and current tests cover targeted regional metric effects, validity checks, save/reload, and classification of 12 authored defense orders. Ten defense orders still have named unavailable consumers, and the tests inject order state rather than exercising every player issue flow. #263 is now `status: partial` with those remaining criteria; #28's live count is 60 open: 37 partial, 14 not started, 2 blocked, 3 deferred, 4 trackers. The earlier inventory sentences in this private chronological ledger are snapshots at their stated moments.

The primary checkout `/root/projects/AHDNative` is now a clean `main` integration worktree at `29ec284` (the latest fetched origin/main at that instant). Its dirty old `src/game/types.ts` shape was superseded by main's richer world-setup/profile types and restored; the three dead links to the removed `muse-phone-final-overflow` worktree were deleted. Detached `c6510ee` is no longer checked out; its one unique, still-relevant public SP auth-isolation test is preserved in draft #683. Its Rust/navigation and old `OnlineSession.test.ts` content were already subsumed or deleted by current main and were explicitly not replayed. The main worktree is a repository anchor, so it is retained as the clean merged integration checkout rather than deleted.

#682 passed hosted verify and merged as `442c118`; #44 received the exact corrupt-image save-load evidence and remains device-blocked. #683 passed hosted verify and merged as `f0dd8b5`. `gh pr merge` returned a local checkout error because `main` was already active in the primary worktree, but GitHub confirmed the remote merge. The task checkout switched to `feat/ahdnative-continuation` from `origin/main`, the merged #683 remote/local branch was deleted, and the primary `main` worktree fast-forwarded cleanly to `f0dd8b5`. #28 now reports 60 open issues and one open PR (#673).

#673's hosted verify run `36096460645` had passed npm verify, engine and content suites, then spent ~40 minutes in the 78-test Playwright smoke step; it was canceled for diagnosis. A focused local 320px browser test reproduced a 90-second timeout clicking the 1991 era radio: its 1px hidden input was covered by the visible label. That red player-flow result led to `1a153af`, which makes each radio fill a 44px label with keyboard focus styling. The earlier attempted local test could not launch because Playwright browser build 1243 was absent; `npx playwright install chromium` installed the matching local browser. The green rerun and 320/390 screenshots are pending, and the fresh hosted verify run `36101077437` is pending. A proposed week-53 rollover fix was withdrawn immediately after correcting the arithmetic (Jan 1 + 364 days stays within the selected year); no date math change was made from that false alarm.

The focused local Chromium rerun after `1a153af` passed both 320px and 390px selector interactions. The scheduler's independently queued visual job `20260925T045616Z-c2e6e9f5` also passed 2/2 at `1a153af`; screenshots were inspected: no horizontal overflow, but the 320px date heading wrapped into three short lines. The same focused local run included real singleplayer create/action/turn/save/relaunch and timed out because character creation displayed `Start date cannot precede the 1953: Cold War Dawn content anchor`. The actual pack starts `1953-01-06`, while the selector's default submitted `1953-01-01`. A new `NewGameScreen` public-flow test was red on Jan 1 and green on Jan 6 after `23a2dd0` carried `listEras().startDate` into `gameChoices` and floored week 1 at that authored date. `23a2dd0` also stacks the date heading at <=360px to keep the year/week legible. A three-case final local browser job `20260925T061826Z-5dae0f58` and fresh hosted run `36102261269` are pending. The previously pending run `36101077437` is superseded by this head; do not use it as current acceptance.

Final local player-flow job `20260925T061826Z-5dae0f58` at `23a2dd0`: both 320px/390px selector cases passed; both screenshots were inspected, with the 320px year/week on one line and no horizontal overflow. The integrated real singleplayer case created the 1953 world, reached Profile, performed an action and advanced to turn 1, then hit Playwright's 90-second test limit while opening the menu before Save. It did not prove save/relaunch. The failure snapshot shows the turn-1 Profile and open game drawer; do not claim the test passed. Hosted full verify `36102261269` is pending. The previously queued stale UI job `20260925T030717Z-77bfb854` failed on a renamed `.ts` test path and is not acceptance evidence. The scheduler started the 20-sample late-game replay `20260925T045356Z-1584b9c1` at 06:28:45 UTC; no result yet.

The 20-sample late-game replay subsequently passed at 06:32:37 UTC on clean source `9e8dcb2` with deterministic output SHA `baa4995d…`. Linux public-engine p50/p95 milliseconds were deserialize 529/1,671, turn 6,379/10,601, serialize 268/556; serialized save 13,776,741 bytes. The host was shared and loaded, so these are not physical-device measurements or a go/no-go. `docs/DEVICE-GATE-RUNBOOK.md` now records the reproducible command, fixture/output hashes, source and summary. The temporary detached benchmark worktree can be removed after the record is committed; #43/#44 remain physical-device blocked.

PR #684 (`a2fc4ba`) contains that public runbook record; #43 and #44 received exact comments and remain blocked. The clean detached `device-gate-benchmark` worktree served only the reproducible replay and was removed after its result was recorded; it had no unique edits to merge. The primary main, task, and #673 PR worktrees remain registered.

PR #685 (`7f7058a`) corrects stale `docs/SAVE-COMPATIBILITY.md` live schema-44 wording to current schema 48, explicitly labels the older v43 run as historical, updates the bounded current projection hash from PR #679, and points progressed political play to the owner-approved successor. `git diff --check` passed; no transfer or mechanics claim. Its hosted verify is pending. PR #684 hosted verify is also pending.

At #673 head `23a2dd0`, hosted verify run `36102261269` passed npm verify, engine/content suites, and full `npm run test:smoke`, which includes the real SP create/action/turn/save/relaunch case. Rust clippy passed; cargo test is in progress. The separate local 180-second rerun job `20260925T063648Z-01960d37` is still queued. The PR body was updated with the current AHDGame SingleplayerHome source comparison (authored era presets retained; Native adds week selection), inspected 320/390 screenshots, TDD evidence, hosted smoke, and the honest local 90-second timeout. #240/#118/#28 remain open.

Hosted #673 run `36102261269` finished success including Rust tests, and PR #673 merged as `1cbfaec` at 06:46:49 UTC. Thus the five original PRs #670–#674 are all merged. Because the hosted full smoke passed at the exact head, the queued duplicate local 180-second run was canceled before dispatch with a recorded reason; it is not a passing local result. The clean `glowing-era-date-selector` worktree and merged local/remote branch were removed. #240 and #118 received exact partial-progress comments and remain open. Only clean primary `main` and this task worktree remain registered.

PR #686 (`27627d5`) records the #673 world-start evidence and exact remaining #240/#118 gaps in `docs/ROADMAP.md`; hosted verify pending. The primary AHDNative `main` worktree fast-forwarded cleanly to `1cbfaec`. A short-lived isolated AHDGame worktree at clean `origin/development` `346f0cdde9` measured the actual Game seed manifest through `tsx`: 379 collections (273 runtime, 48 reference, 58 preserved), versus Native's pinned older `d4baf899` 363-collection contract. #122 received that provenance-drift finding without claiming that older Client has 379 collections. No exporter was implemented in the Game worktree; it was removed cleanly with its empty branch, so no stranded AHDGame checkout remains from this audit.

PR #684 passed hosted verify and merged as `749db82`, and PR #685 passed hosted verify and merged as `450be4c`; #43, #116 and #122 received exact post-merge comments and remain blocked/partial as applicable. Primary `main` fast-forwarded cleanly to `450be4c`. PR #686 is still in hosted smoke.

#51 320px Profile money-wrap slice: an isolated clean branch/worktree was created from `1cbfaec`. The public rendered owner-card flow gained a `Range.getClientRects()` assertion for corporate cash/revenue/share-price values. Scheduler red job `20260925T065935Z-246b2193` failed as intended: Corporate cash occupied two line fragments at 320px. `3ced532` adds a corporation-card-only stacked label/value layout at <=360px. Scheduler green attempt `20260925T070207Z-ef1ba745` passed the 320px owner flow, reverted-owner and ordinary-player cases, but the 390px owner flow hit the global 90-second limit during `page.reload` on this loaded host: full local result 3/4, not a pass. Both width screenshots were inspected; 320px money is whole and 390px keeps the prior columns. `docs/BEHAVIORAL-PARITY.md` records the precise result and remaining CEO/salary/dividend/brand/device gaps. PR #687 is open with hosted verify pending; #51 remains partial. Temporary dependency symlinks were removed from the PR worktree after local testing.

PR #686 passed hosted verify and merged as `cd362e7`; primary `main` fast-forwarded. One bounded Muse Spark 1.3 contributor worked in an isolated AHDGame `feat/native-sp-snapshot-export` checkout for #301 and committed `82cc1fb1cd` locally only. It showed seven fake-Db tests and a synthetic Mongo CLI run, but its proposed exporter serializes entire `gameConfig` documents. The real `GameConfig` type includes Discord webhook URLs and `toPublicGameConfig` exists precisely to remove them. Its separate `ahd-sp-logical-snapshot` contains only `gameConfig`, declares `resumable: false`, and is incompatible with Native's pinned format; it supplies no player state, ruleset identity or meaningful world continuation. The CLI's caller-supplied `--game-revision` can also forge provenance, and its `--live`/env check does not constrain the actual Mongo URI. Reject this local commit and delete its clean worktree/branch: it is not a safe or sufficient #301 exporter, and no code was pushed or PR opened. Keep #301 not started and #122/#116 partial. No production data was touched.

PR #688 (`d89d22e`) corrects three remaining stale save-documentation examples: the Native local export tool now names schema 48 and the current `404370ac…` bounded v42 projection digest, the roadmap no longer calls current output v43, and the v42 compatibility explanation no longer calls current `world.meta.schemaVersion` 44. The source is `packages/engine/src/world.ts` `SCHEMA_VERSION = 48` and the independent old-reader record in `docs/V42-INTERCHANGE-DEPTH.md`; `git diff --check` passed. This is documentation only. Hosted verify is pending; #116/#122 remain partial and neither transfer direction is implemented.

PR #687 hosted verify run `36106271093` passed in full, including `npm run verify`, engine/content suites, full browser smoke and Rust checks. It merged as `3afd9d3` at 07:37 UTC. The Profile corporation money values now stay on one line at 320px in the public rendered assertion; 390px keeps the earlier two-column layout. The local premerge focused browser run remained 3/4 because the 390px reload timed out on the loaded host; the hosted full suite passed. The clean PR worktree and local/remote branch were removed with reason: merged into main, no unique work remains. Primary main fast-forwarded cleanly. #51 remains partial for CEO relation/vacancy, salary, dividends, brand, and physical-device acceptance.

PR #688 hosted verify run `36108174159` passed and merged as `7d0765b` at 07:59 UTC. Primary main and this task checkout fast-forwarded; the PR branch was removed locally and remotely, and its source was already integrated through the squash merge. #116/#122 received post-merge comments and remain partial. No successor transfer is claimed.

The Muse Spark #262 contributor proved a real player command path in isolated branch `feat/min-order-player-flow-262`: presidential create, self-nomination, engine confirmation at turn 24, order issue/rejection/application, save/reload. Its first test commit `37cc1d8` was test-only and local. I replaced its cross-test mutable save dependency with a `beforeAll` public fixture; scheduler job `20260925T073850Z-82273775` passed 5/5 and typecheck job `20260925T074115Z-c7b8c478` passed. I added a Playwright 320/390 integrated drawer/issue/save/resume spec using the same public fixture. Initial job `20260925T074120Z-4dfbfc3a` reached Cabinet office but found an ambiguous test heading locator; interrupted before repeating the known failure. The next job `20260925T074941Z-c6b173de` failed immediately because the interrupted Playwright job left its own Vite server on port 1427; I terminated only that orphan. Job `20260925T075115Z-08dc6abd` then passed both browser widths in 5.6 minutes, and I inspected all four ready/issued screenshots. They exposed a real product defect: issuing an order refreshes `DetailQuery`, remounts `CabinetOfficePanel`, and silently resets the selected Treasury office to the first vacant State office. The shell also displays the same issue notice twice (global plus panel). I added a public browser assertion for portfolio retention and one notice, and scheduler job `20260925T075819Z-bdfb794f` is running red against current code. #262 remains partial until the red/green product fix, hosted verification, and honest reference comparison are complete. No #262 push or PR yet.

#262 integration then exposed a genuine state-retention defect in the local Playwright screenshots: after issue, `DetailQuery` unmounted its cabinet child, reset the selected Treasury portfolio to the first vacant State seat, and duplicated the result notice at shell and panel level. The new 320px public browser assertion ran red in scheduler job `20260925T075819Z-bdfb794f` (two result notices, expected one). The selected-office assertion had raced the async detail reload, so I moved it after the active-order projection became visible. `GameScreen` now holds the selected office across detail revisions and suppresses only duplicate nonbusy cabinet success feedback at the shell; the panel keeps its inline message. The corrected integrated job `20260925T080326Z-a7ba523c` passed 2/2 at 320/390; its ready/issued screenshots were inspected. A concrete footer occlusion risk remained in full-page captures, so a scroll/geometry assertion and active-card screenshots were added. Final scheduler job `20260925T080951Z-4c9ee43c` passed 2/2 and both active-card screenshots showed the complete card above the fixed footer. Typecheck `20260925T081002Z-437a659d` and panel UI suite `20260925T080343Z-8b6a9085` passed, as did `git diff --check`.

The #262 branch was rebased cleanly onto `origin/main 7d0765b` and pushed with commits `b80329d` (natural-flow public test) and `8140501` (UI fix/browser evidence/roadmap). PR #689 is open, hosted verify run `36112225479` pending. Its body uses `Closes #262` only on merge if the hosted gate passes; #105/#281/#43/#44 remain explicit broader gaps. The isolated PR worktree is intentionally retained while the PR is open, with no dirty files.

#122 issue body had stale wording that still described #116 as solely a historical v42 requirement and Native as schema 43. I added an owner-approved 2026-09-25 scope header, corrected the active #116/#122 relationship and current schema 48 references, while preserving dated historical audit text. No transfer result changed. A clean read-only AHDGame audit worktree was created at `origin/development 1919657c62` for a Muse Spark 1.3 safety audit of #301; the contributor has explicit instructions to avoid edits, Mongo connections, env files, credentials, builds and publication. This source revision is newer than the Native v1 snapshot pin `d4baf899`, so its collection policy must not be treated as interchangeable. The audit worktree must be removed when its report is reviewed.

Muse Spark read-only #301 audit finished in `/tmp/ahdnative-muse-snapshot-audit-20260925.md` against AHDGame `1919657c62`; its worktree stayed clean, was removed, and its no-change audit branch was deleted because there was nothing to merge. The audit confirms a resumable 1953 US HoS world needs `characters`, `gameState`, `governmentFormations`, `ministerialOrders`, `counters`, and economy working state, all currently pinned `missing` in Native's v1 collection policy; current Game HEAD also differs from pinned `d4baf899`. `users`/`notifications` remain excluded. A whole `gameConfig` dump would include server/operator fields; future exporters need explicit field allowlists and code-derived revision, never a caller-provided provenance stamp. The report names still-unproven per-turn write set, cabinet seating writer, ruleset hash, client launcher metadata, and re-pin machinery. No transfer code, production data, or credentials were touched. The safe next #301 slice is a fail-closed contract test/export boundary, not a resumable-snapshot claim.

P0 #116 political-state source review: Native `CountryPoliticalOverview` persists approval/history, regime, legitimacy, unrest and updatedTurn under its provisional `countryPolitics` phase. AHDGame `GovernmentApproval` persists approvalRating/disapprovalRating/netApproval, source/history, optional warExhaustion/conflict id and activeNationalModifiers under a different recomputation path. `governmentApprovals` is pinned `missing` in Native v1. This is not a field rename or a frozen opaque extension; any successor must preserve both relevant histories/integrators and advance compatible turn behavior. I added the precise source-backed constraint to #116; no cross-engine runtime result is claimed.

PR #689 passed full hosted verify `36112225479` (npm verify, engine/content, full browser smoke, Rust format/clippy/tests) and squash-merged as `75d3cb4` at 08:48 UTC. `gh pr merge --delete-branch` returned a local-branch deletion error solely because the branch was checked out in its clean worktree; GitHub confirmed the merge. I removed the clean #262 worktree, deleted the local and remote task branch, and fast-forwarded both primary `main` and this task checkout to `75d3cb4`. #262 auto-closed; its final acceptance checkbox was checked and stale `status: partial` label removed. Parent #105 now checks #258–#262 and stays partial for #263. Parent #28 live count was refreshed to 59 open (36 partial, 14 not started, 2 blocked, 3 deferred, 4 trackers, five P0) and zero open PRs; this count is inventory, not untouched-work size. No physical-device or exact cross-engine numeric parity claim was made.

The second Muse Spark #301 implementation contributor is still working in isolated AHDGame branch `feat/sp-snapshot-export-complete-slice` at clean base `1919657c62`. Its uncommitted draft has a v2 exporter and local Mongo tests. Early review found the test world is hand-seeded rather than created/advanced/acted through the public Game flow, optional collections use whole-document export, and economy working state is expressly omitted. Those facts do not satisfy the requested real resumable slice. No code has been pushed or merged; review the final result, reject unsafe/incomplete code or require exact corrections, and resolve this worktree with a written reason.

The second Muse Spark contributor finished and committed local AHDGame `5683246cdc` (1,593 new lines) despite its own stated limitation that it proves only record round-trip, not next-turn continuation. Its Mongo test seeds the claimed character/turn/nomination/order records by hand rather than exercising the public world/player flow; `macroMetrics`, `centralBanks`, `politicalMetrics` and other runtime write-set dependencies are omitted. The optional reference collections are exported as whole documents without a reviewed field policy, and its restore routine checks destination emptiness then inserts collection by collection without atomic rollback. The first full typecheck job `20260925T084405Z-e0dc31ff` failed ten TypeScript errors (mostly string `_id` against ObjectId and exporter shape); the contributor reports later focused/local checks passing, but no full next-turn proof. The code remains local only, with no push, PR or production data. Reject this branch for #301 because it could export an incomplete or unreviewed snapshot and incorrectly label it portable. Remove its clean worktree and local branch with this reason; #301 remains not started for a supported exporter, while the audit report remains useful for design.

For #284, a source-backed 1991 IE VAT public-command test ran red because the law was unavailable (`budget/taxRateLadder`); adding `ie_vat_rate` to the pinned Game catalog generator regenerated its 11 exact options and removed it from the unavailable inventory (267→266). The initial focused run then passed. A second public-command test exposed an existing cross-country authorization hole: a US HoS could sponsor an IE bill through `sponsorCountryId`; scheduler red job `20260925T091315Z-bbc8aa3d` observed `ok: true` where the reference route requires a same-country elected seat or sovereign role. A guard now refunds AP and refuses foreign sponsorship, and the previous JP test's foreign-US sponsoring claim has been replaced with an honest catalog/foreign-refusal test. Green scheduler job `20260925T091456Z-78a0659c` passed its focused IE/JP/inventory tests. Full IE turn/vote/enactment/save continuation job `20260925T091556Z-5d64c4b3` is running. This is in isolated Native worktree `/root/projects/AHDNative/worktrees/ie-vat-law-284`, branch `feat/ie-vat-law-284`; no PR or closure yet. A temporary clean detached Game worktree at pinned `e364c04954` generated the catalog, its `node_modules` symlink was removed, and the worktree was removed cleanly after its sole purpose was complete.

The full IE public-turn test then exposed a real downstream gap: after the bill reached `signed` with a passing NPC vote, VAT remained at 21 rather than moving toward the authored 23; job `20260925T091556Z-5d64c4b3` failed first on missing phase-in target, and job `20260925T091959Z-3ee2a5dd` failed on unchanged rate. A phase trace job `20260925T092355Z-59b24cda` is queued to identify where the enacted tax consequence is lost. The original #283 JP closure relied on a foreign-US sponsorship loophole, so I reopened #283 as `status: partial` with the source-route mismatch and legitimate JP player-flow criteria, and updated parent #101 (also checking genuinely closed #282/#288). This correction raises the live open issue inventory to 60 until a future closure; no player parity is claimed for JP.

IE VAT #284 continuation audit, 09:30–09:54 UTC: the apparent lost 23% enactment was caused by a second autonomous IE VAT bill in the same turn (`bill-1-2-ie_vat_rate`, sponsor `IE-1`) without `selectedRate`, which reset the budget toward 21. AHDGame `proposeNppNationalBill.ts` requires a concrete policy option and `selectNppBill.ts` scores platform distance while excluding current and active duplicate options. The public NPP-turn test failed in scheduler job `20260925T093952Z-5d089a85` on missing `selectedRate`; the bounded option/rate fix passed in `20260925T094229Z-94b27d24`. Full AHDGame agenda/urgency selection remains unported, so #284 is partial. The isolated player bill (NPP autonomy off) passed proposal, votes, signed enactment, 23% phase-in, save/reload and continuation in `20260925T093353Z-a5de0071`. The follow-up public repeal flow passed in `20260925T093529Z-979b8bc7` (that job loaded before the later NPP test was added). The invalid-option AP assertion passed in `20260925T093958Z-1730c950`; no accounting fix was needed because the public executeAction wrapper restores failed charges. Temporary console traces were removed. `ps` found no Devin/SWE-2 process.

IE VAT player reachability audit: `LegislaturePanel` excludes tax laws, while the Legislation route includes them, but its numeric picker snapped irregular authored 23% to 24%. The real Legislation query/param test failed before the option projection in job `20260925T094443Z-d559cf37`; the UI select test failed before the authored-options control in `20260925T094443Z-71de110e`. A subsequent false failure was due to the isolated worktree's node_modules symlink pointing root tests at primary main's engine, so I removed only that symlink and installed local dependencies with offline `npm ci` (scheduler job `20260925T094808Z-829facc9`, passed). The real query/param contract then passed in `20260925T094841Z-0ac12003`, the UI test passed in `20260925T094845Z-2c1e6ba5`, and app typecheck passed in `20260925T095002Z-8b8c8417`. The engine package typecheck job `20260925T094234Z-112e8f13` failed on numerous existing unrelated files and one new missing save timestamp; the new timestamp error was fixed. Current source changes include a source-authored picker and corrected #283/#284 roadmap claims. Full local `npm run verify` job `20260925T095350Z-7c988f0c` and focused engine contracts job `20260925T095355Z-3f6b7c89` are queued/running. The IE worktree remains WIP; no PR, issue closure, paid build, or signing action yet.

IE #284 local verification update, 10:11–10:14 UTC: full scheduled `npm run verify` job `20260925T095350Z-7c988f0c` passed rules and app build, then failed with 902 passed / four timed-out app tests: two `scripts/export-save-v42.test.ts` CLI subprocesses returned `status:null` under their 30-second spawn timeout, and two `src/game/nominations.test.ts` turn tests hit Vitest's 60-second timeout. This full gate is failed, not green. Focused engine public contracts job `20260925T095355Z-3f6b7c89` passed 8/8 (IE real turn/repeal/save, NPP option, JP foreign gate, inventory). A serialized one-worker rerun of only the four-failure files is running in job `20260925T101204Z-0be52e36` to distinguish load effects from regression. App typecheck and build passed. Local commit `cc7c1ae` contains only source/tests/generated catalog/roadmap and the branch is clean; no push or PR yet.

IE #284 timeout adjudication, 10:21–10:27 UTC: isolated one-worker rerun `20260925T101204Z-0be52e36` passed all seven `scripts/export-save-v42.test.ts` CLI tests but reproduced the two nomination 60-second timeouts. A fresh detached clean-main baseline worktree at `75d3cb4`, running only those exact two nomination tests through the scheduler (`20260925T102229Z-b6470b84`), reproduced both timeouts. Therefore these nomination timeouts are present on main under this host/test configuration; the full verify still failed and cannot be reported as passed. The temporary main baseline worktree's only untracked node_modules symlink was removed and the clean worktree was deleted; written reason: read-only baseline comparison complete, no unique code to merge or preserve. The #284 source worktree remains committed and clean pending PR.

PR #690 opened at 10:28 UTC from clean commit `cc7c1ae` (`feat/ie-vat-law-284`) against main. Its description links #284/#283/#101/#28, explicitly says all issues stay open/partial, records source pin and red/green evidence, and discloses the failed full local verify plus clean-main nomination timeout reproduction. It was `MERGEABLE`; hosted verify run `36124140235` started. No Codemagic or signing path was triggered. Wait for hosted verify before merging. The source worktree remains until merge adjudication.

A bounded Muse Spark 1.3 read-only AHDGame #301 source audit completed while PR #690 hosted verification ran. No shell/write/build/service access was given to that contributor; no Devin session was started. The audit identified the local SP sentinel user dependency; I verified directly in AHDGame `src/lib/singleplayerServer.ts` that `ensureSingleplayerUser` upserts a fixed local user with an unusable password marker, and `src/app/api/singleplayer/turn/advance/route.ts` finds the active character by that fixed user id. A future portable snapshot must reconstruct/rebind this local identity without exporting users/passwords/cookies or creating a Native account store. This is a source finding only, not transfer proof. The contributor also warned its wider collection write set is incomplete without live flow instrumentation. No implementation branch was created for #301 in this audit.

PR #690 first hosted verify run `36124140235` failed after app verify on one engine assertion: `catalogPorted.test.ts` still expected `ie_vat_rate` unavailable. This was a true omitted test update, not a runtime parity failure. I corrected the explicit reviewed-row allowlist in local commit `dc74135`; scheduler focused job `20260925T104556Z-0807bf92` passed catalog + inventory + IE player-flow (10/10). Commit pushed to the same PR, with fresh hosted verify run `36125938411` underway. PR body now records the first failed run and correction. `gh pr edit` hit a deprecated Projects classic GraphQL error; `gh api` PATCH updated the body successfully. Do not merge until the new hosted run passes.
