{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "ask",
  "description": "Native Ask panel. The ask window shows local UI only and may call the Ask backend commands, which carry the player's Ask session server-side of the webview boundary. It gets no filesystem, shell, dialog, opener, or updater access.",
  "platforms": [
    "linux",
    "macOS",
    "windows"
  ],
  "windows": [
    "ask"
  ],
  "permissions": [
    "core:default",
    "allow-ask-api",
    "allow-ask-send",
    "allow-ask-stop"
  ]
}
